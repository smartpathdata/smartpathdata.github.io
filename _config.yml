title: SmartPath Data
description: Data consulting and automation for small teams and solo founders.
url: "https://smartpathdata.github.io"

# Disable Minima (or any default theme) so it doesn't inject its own CSS/layouts
theme: null

# Keep markdown rendering
markdown: kramdown

# Exclude dev files from build
exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
